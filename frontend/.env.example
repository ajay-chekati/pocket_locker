# Frontend environment.
# NOTE: anything Vite reads is bundled into the public client. Only put
# non-secret, browser-safe values here (Vite only exposes VITE_* vars).
VITE_API_URL=http://localhost:4000
